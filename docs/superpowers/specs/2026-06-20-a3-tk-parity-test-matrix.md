# A3 Tk Parity Test Matrix

Date: 2026-06-20

Scope: reproduce `D:\quailbot\src\quailbot\calibration` in TypeScript with a web GUI. The only excluded legacy feature is agent workspace activation (`Set agent workspace...` and `Use current workspace for agent`).

Current manual Tk status: Computer Use can launch and control the legacy Tk calibrator. On 2026-06-20 it launched `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\launch_legacy_calibrator.pyw`, found `Workspace Calibrator - workspace.json - idle`, opened the Tk File menu with `Alt+F`, opened the `Load workspace` file dialog, and loaded `D:\quailbot\workspaces\Nanonis-SPM-Controller - Woody.json` without saving it. The Tk success dialog text was `Loaded D:\quailbot\workspaces\Nanonis-SPM-Controller - Woody.json`, and the main title changed to `Workspace Calibrator - Nanonis-SPM-Controller - Woody.json - idle`.

Current visual evidence status: the legacy Tk UI was captured with the desktop screenshot fallback after loading Woody. Evidence files:

- Tk full screenshot: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\tk-printscreen.png`
- Tk cropped calibrator view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\tk-calibrator-woody-crop.png`
- Tk live multi-select crop: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\tk-window-crop-live.png`
- Tk live multi-select preview: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\tk-window-crop-live.preview.png`
- Web final Woody/aprfgen view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-final-woody-aprfgen.png`
- Web Woody multi-select group-edit view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-woody-multiselect-group-edit.png`
- Web live field edit and undo/redo view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-live-form-undo-redo.png`
- Web CLI metadata live-edit view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-meta-live-edit.png`
- Web final ready view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-final-ready.png`
- Web empty-selection form view: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-empty-selection-form.png`
- Web ROI form without placeholder delete button: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-roi-form-no-placeholder-delete.png`
- Web Group form disabled geometry fields: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-group-disabled-geometry-fields.png`
- Web Anchor form disabled Width/Height fields: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-anchor-disabled-wh-fields.png`
- Web CLI form disabled Name field: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-name-disabled.png`
- Web CLI action linked frame without CLI Actions/hint: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-action-no-actions-hint.png`
- Web CLI parameter linked frame with disabled CLI Actions/hint: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-param-actions-hint.png`
- Web linked-observables listbox with Remove selected: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-linked-listbox-remove-selected.png`
- Web linked-observables selected listbox row: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-linked-listbox-selected.png`
- Web linked-observables after Remove selected: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-linked-listbox-after-remove.png`
- Web Tk-style File dropdown menu: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-tk-style-file-menu-final.png`
- Web Tk-style Help menu dialog: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-tk-style-help-menu-dialog-final.png`
- Tk Computer Use fallback screenshot after helper repair: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\tk-computer-use-fallback-fullscreen-items-filter.png`
- Web final File dropdown with latest CSS: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-9295-file-menu-open.png`
- Web final Current item selected with menu closed: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-9295-current-selected-menu-closed.png`
- Web 1280px ROI drag after layout/capture/overlay fixes: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-chrome-roi-drag-outline-versioned-capture.png`
- Web 1280px Anchor pick after layout/capture/overlay fixes: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-chrome-anchor-pick-outline-versioned-capture.png`
- Web group collapse retained after rename: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-group-collapse-rename.png`
- Web filter checkbox click and disappearing-tag prune: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-filter-checkbox-prune.png`

## Parity Domains

| Domain | Legacy authority | Tk behavior to observe | Web behavior/test target |
| --- | --- | --- | --- |
| Window shell and menus | `gui.py:_build_ui` lines 924-1114 | File menu has Load/Export only for A3; Help opens the canonical help text. Main panes are canvas, items, selected form. | Browser shell must keep canvas/items/form order, no activation commands, no global controls in selected form. |
| Items list order and labels | `_refresh_list`, `_format_item_label`, `_insert_list_item` lines 1364-1480 | Groups render recursively, collapsed groups hide children, rows display `[x]/[ ] [TAG] name`, CLI tag uses CLI name. | DOM tree rows and visual screenshot match ordering, indentation, active labels, collapsed behavior. |
| Extended selection and toggles | `_on_items_click`, `_on_items_double_click` lines 2208-2285 | Body click selects; Ctrl toggles; Shift range selects; explicit left toggle changes active; group toggle cascades; forced ROI locks. | Browser event tests and in-app browser runs cover body/toggle/ctrl/shift/double-click/locked ROI. Regression gate: visible rows such as `[nqctl] Current` must be pointer/click selectable, not static text. |
| Filter panel | `_refresh_filter_tags`, `_item_matches_filter`, `_match_terms` lines 1571-1683 | Tag checkbox list sorted by count/name with tag text only; keyword comma terms; AND/OR toggle; Clear resets; child visible when descendant matches. | Shared filter tests plus browser DOM tests verify tag, keyword, subtree visibility, no count text. |
| Add/delete items | `_add_roi`, `_add_anchor`, `_add_group`, `_add_cli_param`, `_delete_selected` lines 2824-3019 | Add creates deduped defaults and selects item; delete confirms and removes selected items, cleaning links/groups. | Toolbar tests plus browser flow verify defaults, selection, confirmation, cleanup. |
| Selected form field visibility | `_on_select`, `_set_geometry_fields_visible`, `_set_linked_frame_mode`, `_set_cli_meta_visible` lines 1826-2479 | None shows empty; multi shows count and group-only edit; ROI shows x/y/w/h; anchor shows x/y and linked ROI picker; group shows name/description/tags/group; CLI hides geometry and shows metadata/link controls. | Form render tests plus browser screenshots per kind. |
| Field editing and undo/redo | `_set_field`, `_record_entry_history`, `_on_entry_undo`, `_on_entry_redo`, `_on_desc_undo`, `_on_desc_redo`, `_on_form_changed` lines 2481-2822 | Edits update draft/list labels live; Ctrl+Z/Y restore text and cursor; selection change resets history; group rename cascades; cycle guard warns. | DOM form tests for live edits, undo/redo, group cascade, `(mixed)`, cycle rejection. |
| Linked observables | `_refresh_roi_options`, `_add_linked_roi`, `_remove_linked_roi`, `_set_linked_rois_ui`, `CliParamDraft.runtime_linked_observables` lines 125-150 and 1541-1824 | Anchor linked ROI picker/search/add/remove; CLI auto self-observable appears non-editable; explicit links editable only for set/action entries. | Linked-observable tests cover anchor, CLI parameter/action, auto vs editable, forced ROI activation. |
| CLI metadata form | `_populate_cli_meta`, `_set_cli_*`, `CliParamDraft.sync_actions_from_metadata`, `to_json` lines 152-256 and 1881-2064 | Parameter get/set/ramp checkboxes are derived/display-only; writable appears only with `set_cmd`; action safety mode only for actions; safety fields enabled only when present; payload preview reflects serialized JSON. | CLI metadata visibility/edit/payload tests plus browser selected CLI inspection. |
| File load/export/save | `_load_workspace_dialog`, `_export_workspace_dialog`, `_load_workspace`, `_write_workspace`, `_save` lines 3021-3260 | Load reads any selected JSON; Save writes current path and keeps current; Export writes chosen path and does not change current path; status/message behavior visible. | Server route and browser file-browser flow tests prove load/save/export and path/hash readback. |
| Workspace parse/serialize | `_load_workspace_raw`, `_parse_cli_parameter_drafts`, `_parse_cli_params_block`, `_serialize_cli_tools`, `_serialize_cli_params_block` lines 282-663 | Missing file shape, GUI wrapper precedence, legacy `tools.cli`, modern `cli_params`, dict fallback, bool coercions, linked field precedence, both CLI representations written. | Python-golden fixture tests compare TypeScript against committed outputs. |
| Save validation and normalization | `_write_workspace`, `_apply_forced_roi_activation`, `_delete_selected_items` lines 665-732 and 1517-1534 and 3167-3257 | Names required/unique across all kinds; ROI w/h positive; linked ROIs filtered; linked observables deduped; orphan groups reset; cycles error; forced active ROI applied before save. | Shared validation/serialize tests plus save route 4xx tests. |
| CLI import | `cli_import.py` and `gui.py:_load_params_from_cli` lines 2892-3000 | Probe `capabilities`, fallback to `capacities` only on non-zero; reject bad CLI; merge by `(CLI_Name,name)`; identical skip; conflict asks keep/use-loaded/cancel; report lists field diffs. | Shared/server/client CLI import tests plus browser modal flow. |
| Canvas capture and coordinates | `capture.py:ScreenCapturer`, `_refresh_screenshot`, `_render_canvas_image`, `_canvas_to_screen`, `_screen_to_canvas` lines 3288-3360 | Capture merged virtual screen physical pixels; canvas fit never upscales; screen/canvas transforms use truncation and origin; scrollbars reflect image. | Capture unit tests, geometry tests, in-app browser asset/pixel tests. |
| Canvas interactions | `_draw_existing_roi`, `_draw_existing_anchor`, `_on_zoom`, `_on_mouse_down`, `_on_mouse_move`, `_on_mouse_up` lines 3376-3528 | ROI rectangle and anchor crosshair render over screenshot; draw ROI drag writes x/y/w/h; pick anchor click writes x/y; Ctrl-wheel zoom keeps pointer; wheel pans; draw/pick suppress zoom. | Browser canvas event tests and real-browser visual tests for overlay/image alignment. |
| Form and canvas scrolling | `_on_form_mousewheel`, `_on_preview_mousewheel`, `_on_preview_shift_mousewheel`, `_on_preview_alt_mousewheel` lines 2116-2150 | Form wheel scrolls form; canvas wheel pans vertical, Shift/Alt horizontal; handlers consume event. | Browser wheel tests scoped by region. |
| Help | `_show_help_tooltip` lines 2153-2182 | Help text literal opens in a transient window and closes on Escape/focus loss. | Web help modal/toolbar test must pin literal text; activation help items excluded. |

## Explicit Non-Goals

- Agent workspace settings writes: `gui.py` lines 3046-3146 are excluded from A3.
- Browser-only activation request surfaces remain regressions: no `/api/request-activation`, no `pendingWorkspaceActivation`, no activation UI.

## Evidence Gates

1. Python-golden fixtures cover pure parse/serialize/model/CLI merge behavior.
2. Vitest + jsdom covers event wiring and emitted workspace state.
3. In-app Browser covers real rendered layout, visual non-overlap, canvas image visibility, and interaction flows.
4. Computer Use covers legacy Tk interaction observations and desktop screenshot evidence.

## Current Tk Observations

- File menu opens via `Alt+F`; the exposed menu item order matches source order: Load workspace, Export workspace, separator, Set agent workspace, Use current workspace for agent. A3 Web keeps Load/Export and drops the two agent workspace entries by design.
- `Load workspace` opens a native Windows file dialog titled `Load workspace`.
- Loading `D:\quailbot\workspaces\Nanonis-SPM-Controller - Woody.json` shows a `Workspace loaded` modal with OK and text `Loaded D:\quailbot\workspaces\Nanonis-SPM-Controller - Woody.json`.
- After acknowledging that dialog, the Tk window title is `Workspace Calibrator - Nanonis-SPM-Controller - Woody.json - idle`.
- The loaded Tk layout shows a left screenshot/canvas pane with scrollbars, a middle item list/filter/toolbar pane, and a right selected-item form. Initial Woody rows include `[GROUP] Z Controller`, nested controller groups, and `[nqctl] ...` CLI entries without list bullets.
- Tk source observation for multi-select: `_on_select` renders `Multiple items (N)`, keeps the geometry fields visible but empty/disabled, disables description and linked/CLI controls, and leaves only the Group combobox enabled. `_on_form_changed(source="group")` applies the selected group to every selected ROI/anchor/CLI/group and rejects cycles with `Invalid group`.
- Tk live multi-select observation: the desktop screenshot fallback found the Tk window by title, cropped its real window bounds, and captured `Multiple items (2)` with empty disabled Name/X/Y/Width/Height/Tags/Description fields and only the Group combobox enabled. This is the Tk-side visual baseline for the Web multi-select regression.
- Computer Use can discover and activate the Tk window. The Tk body accessibility tree remains mostly anonymous panes, so body UX still needs screenshot-backed observation rather than text-only automation.
- The local Computer Use fallback helper was repaired after observing that opaque Computer Use window ids are not Win32 HWNDs and GDI `CopyFromScreen`/`BitBlt` can fail with `The handle is invalid`. The helper now has a Win32 `VK_SNAPSHOT` / Clipboard fallback. On 2026-06-21, `get_window_state({ include_screenshot: true, include_text: true })` against `Workspace Calibrator - workspace.json - idle` returned a real 2560x1440 desktop screenshot showing the Tk item/filter pane. Exact Tk window cropping remains unresolved when UIAutomation bounds lookup fails, but the screenshot is no longer blank or unavailable.
- Fresh Tk relaunches are discoverable through Computer Use as `Workspace Calibrator - workspace.json - idle`, and `Alt+F` exposes the Tk menu bar in the accessibility tree as `File` and `Help`; opening File exposes four menu items, where A3 keeps only Load/Export and excludes the two agent-workspace commands.
- The allowed sample files `D:\quailbot\workspace.example.json` and `D:\quailbot\workspaces\Nanonis-SPM-Controller - Woody.json` are observation/load inputs only; save/export tests must use a temp copy or a new temp path and must not overwrite those files.
- Computer Use against `Workspace Calibrator - Nanonis-SPM-Controller - Woody.json - idle` opened the real Tk `Load workspace` native Windows dialog. The main calibrator window became disabled while the dialog was present, the dialog exposed standard `Open` and `Cancel` controls, and `Escape` closed it without loading or writing files. This establishes the Web file browser needs to preserve common native file-dialog activation behavior, including opening a selected file through a fast second activation/double-click path.
- Tk save-validation source authority: `_write_workspace` stops on the first validation failure and passes `str(e)` to `messagebox.showerror(f"{action} failed", str(e))`; duplicate names use Python `!r` formatting such as `Duplicate name: 'same'`, ROI dimension failures use `ROI 'bad' must have positive w/h`, and group cycles use `Group cycle detected at 'a'`. On 2026-06-21 Computer Use confirmed a real temporary Tk window titled `Workspace Calibrator - tk-invalid-duplicate-workspace.json - idle` launched from an ignored duplicate-name fixture. The Computer Use screenshot fallback could enumerate/capture the window, but coordinate input against the fallback-gdi screenshot repeatedly returned `call get_window_state before issuing coordinate input`; this pass therefore uses Tk source for exact error text and the real Tk window launch as GUI-presence evidence rather than claiming a completed Tk click-through of the error modal.

## Current Web Observations

- Final verified URL: `http://127.0.0.1:1032/?token=6cfbb4d2565e67d5b573128d040c7300da091e6f0bbb24dd`.
- Current menu-parity verification URL after the latest server restart: `http://127.0.0.1:9295/?token=7239712b4704f04583d937c3d8998b5f672c7aed1fc35a93`.
- Browser checks: page title `Quailbot Workspace Calibrator`, root `data-workspace-ui-ready=true`, boot step `ready`, no console warnings/errors from the app.
- Final Browser check: Load/Export menu buttons are present, activation/agent-workspace buttons are absent, startup rows render, and console warn/error logs are empty. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-final-ready.png`.
- Menu parity: Web top bar now matches Tk's menubar shape with only `File` and `Help` visible at rest. Clicking File opens a dropdown below the menu bar containing only `Load workspace...` and `Export workspace...`; global Escape and outside item clicks close the dropdown; clicking Help opens `Show help`, and `Show help` opens the canonical transient help dialog. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-9295-file-menu-open.png`, `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-9295-current-selected-menu-closed.png`, and `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-tk-style-help-menu-dialog-final.png`.
- Menu keyboard accelerators: Tk accepts the Windows menu path `Alt+F` / `Alt` then `F`, then focuses the first File menu item. Computer Use against `Workspace Calibrator - Nanonis-SPM-Controller - Woody.json - idle` verified that `Alt+F` followed by `Down` enters the File menu and focuses its first menu item (`menu item ID: 13` in UIAutomation). Web now supports both chord and Alt-prefix sequences; Browser evidence on `http://127.0.0.1:10996/?token=34a448df97473b206f272bb827220aaceff2bbbbb66e6f5f`: Browser key input `ALT,f` opens File with `aria-expanded=true`, `fileHidden=false`, and focus on `Load workspace...`; `ALT,h` switches to Help with focus on `Show help`; console warn/error logs are empty. Browser CDP screenshot capture timed out, so evidence for this pass is DOM/key-state readback rather than screenshot.
- Help: `Show help` opens the canonical workflow/preview-controls text and Close/Escape paths are covered by tests.
- Items: `[nqctl] Current (current)` is selectable; selection moves to the row and the right form changes to CLI Parameter. The latest browser regression explicitly opened File, closed it through Escape/outside click, clicked Current, verified `aria-selected=true`, and verified the menu remained closed. Add ROI creates and selects `new_roi`; Delete becomes enabled and Draw ROI box becomes enabled for a single ROI.
- Group collapse: real Browser verification found that Playwright/Browser double-click on a Web group did not collapse children because the first pointer activation selected the group and re-rendered the row before the native `dblclick` path could run. Web now treats repeated activation of the same group body as the Tk double-click collapse path and suppresses duplicate native `dblclick` toggles. Browser evidence on `http://127.0.0.1:4307/?token=6cce5ff1b4b6fc550d7f02a3ad2c44d62654a00a721af847`: Add Group, Add ROI as child, double-click group, rename `new_group` to `browser_group`; final DOM rows contain `group:browser_group` and no `roi:new_roi`, proving collapsed state remained attached across rename like Tk's `GroupDraft.collapsed`.
- Filter checkbox UX: real Browser verification found Web checkboxes could be rendered but not user-clickable because generic pointer activation competed with checkbox default behavior, and the keyword row incorrectly used a `<label>` around the input plus AND/Clear buttons. Web now keeps keyword row as a plain `div`, handles checkbox pointer activation explicitly for filter/toolbar/CLI metadata controls, and makes filter tag changes idempotent using the checkbox checked state like Tk's `BooleanVar`. Browser evidence on `http://127.0.0.1:7963/?token=2e48aa813c19aa62023f7c08f37e067e06d07f84ec4969b9`: Add ROI, set tag `r1`, click `r1`; final rows reduce to only `[ROI] new_roi` and the `r1` checkbox is checked. Renaming the tag to `renamed-tag` removes the old invisible selected tag and restores all rows with `renamed-tag` unchecked, matching Tk's `_refresh_filter_tags` behavior.
- Form wheel scope: Tk wraps the selected-item form in its own `Canvas`/vertical scrollbar, binds `<MouseWheel>` to `_on_form_mousewheel`, scrolls the form canvas with `yview_scroll`, and always consumes the event with `return "break"`. Web now attaches a non-passive wheel handler to `[data-form-root]`, normalizes pixel/line/page deltas, updates only the form scroll position, and prevents body/page scrolling. Browser evidence on `http://127.0.0.1:9278/?token=4dcb91a46232fa20591ca172d586559b45bcaa5568545aa5`: selecting `current` made the CLI form scrollable with max scroll `216`; wheel input over the form changed `formScrollTop` `0 -> 216 -> 0` while `bodyScrollTop` stayed `0`. The Browser CDP scroll helper timed out after dispatching the gesture, but DOM readback proved the UI state changed correctly.
- Load: File -> Load workspace opens the web file browser, enters `.quailbot-pi\workspaces`, selects `Nanonis-SPM-Controller - Woody.copy.json`, and loads 447 rows without overwriting the original Woody JSON.
- Filter: keyword `scan` reduces Woody from 447 rows to 32 rows, then Clear restores the full tree.
- Selection after load: selecting `aprfgen_freq` shows the CLI Parameter form, linked observables, CLI actions, description, safety fields, metadata JSON preview, and disabled draw/pick buttons for CLI rows.
- Capture: Refresh screenshot returns `/assets/workspace-capture?captureId=...`, canvas image is visible, and no user-facing capture error is shown.
- Capture asset stability: the Web server now stores immutable `workspace-capture.<captureId>.png` files and serves `/assets/workspace-capture?captureId=...` from the matching versioned file before falling back to the legacy current metadata path. This fixes the observed race where a later refresh overwrote `workspace-capture.metadata.json` before the browser loaded the previous SVG image href, producing 404s. Real Chrome verification on `http://127.0.0.1:12645/?token=338a387a815fc17bb0b4564e57751c02e239d5d8a65250cb` saw two capture image responses, both 200, with no `requestfailed` events and no console logs.
- Canvas layout at 1280px: the shell now keeps all three panes visible with bounded grid columns instead of squeezing the form pane down to labels. Chrome evidence shows the canvas, items/tool pane, and selected form all visible at 1280x900.
- Canvas overlay parity: Tk uses `create_rectangle(... outline="#00d1ff", width=2)` for ROI boxes and yellow `#ffcc00` line/oval strokes for anchors. Web now renders only the single selected ROI or Anchor overlay, suppresses overlays for multi-selection/non-overlay selections, and sets SVG `fill="none"` with the same cyan/yellow stroke colors. The previous solid black ROI fill was a Web bug and is fixed.
- Canvas interaction parity: after `Add ROI`, `Refresh screenshot`, and `Draw ROI box`, Browser-driven drag writes ROI X/Y/Width/Height into the selected form and shows the cyan outline over the screenshot. Verified Chrome state: ROI fields `X=766`, `Y=473`, `Width=1382`, `Height=868`, one ROI overlay, zero Anchor overlays, `fill=none`, `stroke=#00d1ff`. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-chrome-roi-drag-outline-versioned-capture.png`.
- Canvas anchor parity: after `Add Anchor` and `Pick anchor point`, Browser-driven click writes Anchor X/Y into the selected form, disables Width/Height, and shows only the yellow anchor crosshair/circle. Verified Chrome state: Anchor fields `X=2454`, `Y=778`, Width/Height disabled and empty, zero ROI overlays, one Anchor overlay, `fill=none`, `stroke=#ffcc00`. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-chrome-anchor-pick-outline-versioned-capture.png`.
- Canvas form synchronization: draw/pick commits now clear stale form buffers and history so the selected form updates immediately after canvas writes workspace coordinates, matching Tk's `_on_select(populate_form=True)` refresh after draw/pick.
- Browser input compatibility: canvas handlers now accept ordinary mouse events in addition to pointer events. This covers the real Browser/Computer-Use-style input path that previously updated neither ROI nor Anchor reliably when only pointer events were wired.
- Canvas preview wheel parity: Tk binds `<MouseWheel>`, `<Shift-MouseWheel>`, `<Alt-MouseWheel>`, and `<Control-MouseWheel>` separately: ordinary wheel pans vertically, Shift/Alt wheel pans horizontally, Ctrl-wheel zooms around the pointer, and draw/pick modes consume Ctrl-wheel without zooming. Web now has jsdom event regressions for those branches in `tests/workspace-ui/dom/canvas-events.test.ts`. Browser validation on `http://127.0.0.1:14600/?token=d16da236e493e23b12b057a06712910fa08242e57224f456`: page loaded with title `Quailbot Workspace Calibrator`, Refresh screenshot produced a canvas image asset `/assets/workspace-capture?captureId=...`, and console warn/error logs were empty. The Browser Chrome CDP `Input.synthesizeScrollGesture` path timed out when asked to synthesize Ctrl-wheel and did not change DOM state, so the wheel-specific proof is the DOM event regression rather than a Browser scroll gesture.
- Multi-select: selecting `current`, then Ctrl-selecting `Scan_Action`, produces `Multiple items (2)` with both rows highlighted. The right form now shows empty disabled Name/X/Y/Width/Height/Tags/Description controls and an enabled Group selector, matching the Tk multi-select branch. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-multiselect-disabled-fields.png`.
- Multi-select group edit on Woody: File -> Load workspace was exercised through the Web file browser, selecting `Nanonis-SPM-Controller - Woody.copy.json` from `.quailbot-pi\workspaces` and loading 447 visible rows. Browser clicks selected `current`, Ctrl-selected `zctrl_setpoint_a`, changed the Group selector to `Scan Controller`, and verified both selected rows moved to depth 1 under `Scan Controller` while selection stayed active. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-woody-multiselect-group-edit.png`.
- Live field edits and per-field undo/redo: Tk binds form entries/textareas to `<KeyRelease>`, so Web now commits ordinary field edits immediately instead of waiting for blur. Browser clicks Add ROI, fills `browser_roi`, verifies the selected tree row changes before blur, then Ctrl+Z restores `new_roi` and Ctrl+Y restores `browser_roi`. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-live-form-undo-redo.png`.
- CLI metadata live edits: Tk binds `get_cmd`/`set_cmd` descriptions and safety numeric entries to `<KeyRelease>`. Web now commits these on input instead of waiting for blur. Browser selects `zctrl_setpnt`, fills `get_cmd description` with `browser get desc`, and verifies the live payload preview immediately includes `get_cmd.description`. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-meta-live-edit.png`.
- CLI Name entry parity: Tk creates `CLI Name` as a real `ttk.Entry(textvariable=_cli_name_var)` and `_load_params_from_cli` reads `_cli_name_var.get()`. Web previously used a `span contentEditable role=textbox`, which was not a native input control. Web now renders `input[data-cli-import-name="true"][type=text]` and reads `input.value` for `/api/cli-import`. Browser evidence on `http://127.0.0.1:13172/?token=c42e89667fcd411bb1fda23436c7984edb11e0d8fdad37b3`: filling `customctl` retained `value="customctl"` in the native input; clicking `Load Param From CLI` returned `spawnSync customctl ENOENT`, proving the edited value was the CLI name used for probing; console warn/error logs were empty.
- Empty selection form: Tk `_on_select` keeps the right-side form shape visible when no item is selected: empty disabled Name/X/Y/Width/Height/Tags/Description fields and a disabled Group combobox set to `(none)`. Web now renders the same disabled empty form instead of a placeholder sentence. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-empty-selection-form.png`.
- Right-panel action placement: Tk keeps Delete in the items/tool button area, not inside the selected-item form. Web removed the stale disabled `Delete Selected`/`Wired in next phase` placeholder from ROI/Anchor/Group forms; Browser verifies an ROI form exposes only the expected fields and no buttons. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-roi-form-no-placeholder-delete.png`.
- Single-selection field states: Tk `_on_select` keeps disabled geometry fields visible for Anchor (`Width`/`Height`) and Group (`X`/`Y`/`Width`/`Height`), and disables the CLI `Name` entry while leaving CLI Tags/Group/Description editable. Web now mirrors those states. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-group-disabled-geometry-fields.png`, `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-anchor-disabled-wh-fields.png`, and `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-name-disabled.png`.
- CLI linked frame mode: Tk shows the disabled `CLI Actions` get/set/ramp display and auto-observable hint only for CLI parameter mode, not for CLI action mode. Web now mirrors this: `Scan_Action` renders `Linked Observables (cli action)` without the hint/actions frame, while `zctrl_setpnt` renders parameter mode with the hint/actions display. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-action-no-actions-hint.png` and `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-cli-param-actions-hint.png`.
- Linked-observable removal shape: Tk uses one extended-select listbox plus a single `Remove selected` button; Web now uses the same shape instead of per-row remove buttons. Browser adds `new_roi` to an Anchor linked list, selects the listbox row, verifies `role=listbox`, `aria-multiselectable=true`, no per-row remove buttons, and removes it through `Remove selected`. Browser evidence: `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-linked-listbox-selected.png` and `D:\quailbot-pi\.opencode\artifacts\a3-browser-tdd-current\web-a3-linked-listbox-after-remove.png`.
- File browser load double activation: Tk delegates Load to the native Windows file dialog; Web now mirrors the native double-click path by treating a fast second activation of the same selected file in load mode as `Open`, while export mode remains explicit-save only. Browser validation on `http://127.0.0.1:8480/?token=8e85e2fab1401071afdecf12491ab80932513e05eec39571`: File -> Load workspace -> `.quailbot-pi\workspaces` -> double-click `Nanonis-SPM-Controller - Woody.copy.json` closed the modal and loaded the Woody tree (`Z Controller`, `Current`, `Scan_Action`, etc.) with zero console warnings/errors. IAB localhost navigation timed out and Chrome `Page.captureScreenshot` timed out, so this pass uses Chrome extension DOM/state evidence rather than screenshot evidence.
- File browser focus and Escape: Tk uses native file dialogs for Load/Export, so the dialog owns focus and Escape cancels without loading or writing. Web now focuses the custom file browser `role=dialog` on render and also handles document-level Escape only while the file browser is open. Browser validation on `http://127.0.0.1:1786/?token=9c66ca8ff4f14f50fe1ca190bccb67bc336bb47e9b834fdb`: File -> Load workspace opened a dialog whose active element was `SECTION.file-browser-modal` with `role=dialog`; pressing Escape closed it (`modalCount=0`, `dialogCount=0`) and console warn/error logs stayed empty. IAB localhost navigation timed out and reset the Browser session; Chrome extension backend produced DOM/focus/keyboard evidence.
- File operation success feedback: Tk `_load_workspace` shows `Workspace loaded` with `Loaded {path}`, and `_write_workspace` shows `Saved to {path}` / `Exported to {path}` through `messagebox.showinfo`. Web now mirrors that success-feedback UX through the same `window.alert` path already used for other Tk `showinfo` ports. Browser validation on `http://127.0.0.1:10499/?token=693b750f267f462e28d3f7bf7d7c55ecb8f6782b70903549`: File -> Load workspace -> Parent -> `[DIR] workspaces` -> select `Nanonis-SPM-Controller - Woody.copy.json` -> Open produced a JavaScript `alert`; after dismissing it, the load modal was closed and the tree showed the Woody workspace (`Z Controller`, `Bias Control`, `Current`, `Scan_Action`, etc.) with zero console warnings/errors. Browser API exposed the dialog type but not alert text, so exact alert text is covered by jsdom tests for Load, double-activation Load, Save, and Export. IAB navigation and Chrome `Page.captureScreenshot` both timed out in this pass, so evidence is DOM/dialog/console readback rather than a new screenshot artifact.
- File operation failure feedback and validation messages: Web save validation now mirrors Tk's first-failure behavior and Python-style string repr in user-facing messages. `/api/save` returns the first concrete validation message (for example `Duplicate name: 'current'`) instead of the generic `workspace validation failed`, and the client alerts Load/Save/Export failures like Tk `showerror`. This also fixes the prior toolbar Save failure path where `fileBrowser.open=false` meant the error was stored in state but not visible. Browser validation on `http://127.0.0.1:12629/?token=ae7536fde8027497abe1384aba58531c1686ba8472ac6231`: Add ROI -> edit Name to `current` -> Save produced a JavaScript `alert`; after dismissal the app stayed usable, the duplicate `[ROI] current` row remained selected, and console warn/error logs were empty. IAB localhost navigation again timed out and reset the Browser session; Chrome extension backend was used for the DOM/dialog/console evidence. Exact error text is covered by shared validation, server route, and jsdom file-browser tests.
- Export default filename: Tk `_export_workspace_dialog` pre-fills `initialfile` with `self.workspace_path.name`, falling back to `workspace.json` when there is no current workspace path. Web now mirrors that in reducer state before the file browser renders. Browser validation on `http://127.0.0.1:13642/?token=4c807b5f6575b6b6a515c194a9734644b677a0fdf2928018`: File -> Export workspace opened `Export workspace`, showed `Filename` as `workspace.json`, and console warn/error logs stayed empty. IAB localhost navigation timed out and reset the Browser session; Chrome extension backend produced DOM/role/value evidence. Chrome `Page.captureScreenshot` also timed out, so no new screenshot artifact was written.

## Current Automated Gates

- `npx vitest --run tests/workspace-ui/dom/file-browser-flow-save-export.test.ts tests/workspace-ui/dom/file-browser-cancel.test.ts tests/workspace-ui/dom/file-browser-render.test.ts --reporter=dot`: 3 files, 8 tests passed on 2026-06-21 after the export default filename parity fix.
- `npx vitest --run tests/workspace-ui/dom/file-browser-cancel.test.ts tests/workspace-ui/dom/file-browser-flow-load.test.ts tests/workspace-ui/dom/file-browser-flow-save-export.test.ts tests/workspace-ui/dom/file-browser-render.test.ts --reporter=dot`: 4 files, 11 tests passed on 2026-06-21 after the file browser focus/document-Escape parity fix.
- `npx vitest --run tests/workspace-ui/dom/canvas-events.test.ts --reporter=dot`: 1 file, 7 tests passed on 2026-06-21 after adding canvas preview wheel/zoom parity coverage.
- `npx vitest --run tests/workspace-ui/dom/file-browser-flow-load.test.ts tests/workspace-ui/dom/file-browser-flow-save-export.test.ts tests/workspace-ui/server/server.test.ts tests/workspace-ui/shared/validate.test.ts --reporter=dot`: 4 files, 31 tests passed on 2026-06-21 after the save-validation error parity fix.
- `npx vitest --run tests/workspace-ui --reporter=dot`: 49 files, 228 tests passed on 2026-06-21 after adding canvas preview wheel/zoom parity coverage. One immediate prior run saw `tests/workspace-ui/dom/bundle.test.ts` read an incomplete bundle and fail with `Unexpected end of input`; rebuilding `dist/workspace-ui/client.js` and rerunning `bundle.test.ts` passed, then the full workspace-ui subset passed.
- `npm test -- --reporter=dot`: 91 files, 508 tests passed on 2026-06-21 when rerun outside the sandbox after sandboxed config loading hit `EPERM` writing `node_modules\.vite-temp`.
- `npm run build`: passed TypeScript, browser typecheck, and workspace UI client bundle build on 2026-06-21.
- Added interaction regression tests for late bundle bootstrap, row blank-space click, pointer activation fallback, repeated group pointer activation collapse across re-render, no double execution after pointerup+click, Help modal/dropdown close behavior, File Load/Export menu flow, favicon noise suppression, Refresh screenshot error text, Tk-style multi-select disabled fields, group collapsed-state rename/delete parity, and removal of activation-request residue.
